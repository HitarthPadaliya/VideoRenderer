void ABuildingManager::UpdatePreviewLocation(FVector RayPosition)
{
    if (PreviewBuilding != nullptr)
        return;

    PreviewBuilding->SetActorLocation(RayPosition, false, nullptr, ETeleportType::None);
}